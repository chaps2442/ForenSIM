# ForenSIM V2 Dependencies
# Use `pip install -r requirements.txt` to setup the environment

customtkinter>=5.2.2
pyscard>=2.0.7

# --- Moteur pySim (Osmocom) ---
# pySim >= 2024 a deplace ses utilitaires internes dans un paquet PyPI
# separe : "pyosmocom" (module importe sous le nom "osmocom").
# Sans lui, pySim-shell.py plante avec :
#   ModuleNotFoundError: No module named 'osmocom'
# ... en pleine extraction, ce qui produit un rapport vide et un scelle
# trompeur. On l'installe donc explicitement.
# (Alternative : lancer `pip install -r requirements.txt` DANS le dossier
#  pySim que vous avez clone, ce qui tire toutes ses dependances.)
pyosmocom>=0.0.3
# ForenSIM V2 Dependencies
# Use `pip install -r requirements.txt` to setup the environment

customtkinter>=5.2.2
pyscard>=2.0.7
